{@user.email} has transferred {@site.domain} to the "{@team.name}" team on Plausible Analytics.
<a href={Routes.stats_url(PlausibleWeb.Endpoint, :stats, PlausibleWeb.SitePath.encode(@site.domain), []) <> "?__team=#{@team.identifier}"}>
  Click here
</a>
to view the stats.
